import {createServerSupabaseClient} from "@/lib/server"; import PropertyImages from "@/components/PropertyImages"; export const dynamic="force-dynamic"; export default async function Page({params}:{params:Promise<{id:string}>}){const {id}=await params;const s=await createServerSupabaseClient();const [{data:p},{data:imgs}]=await Promise.all([s.from("properties").select("*").eq("id",id).single(),s.from("property_images").select("*").eq("property_id",id).order("sort_order")]);return <PropertyImages property={p} images={imgs||[]}/>;}