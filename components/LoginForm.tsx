"use client"; import {useState} from "react";import {createClient} from "@/lib/supabase";import {useRouter} from "next/navigation";export default function LoginForm(){const [e,setE]=useState("");const [p,setP]=useState("");const [err,setErr]=useState("");const r=useRouter();async function go(ev:React.FormEvent){ev.preventDefault();setErr("");const {error}=await createClient().auth.signInWithPassword({email:e,password:p});if(error)setErr("邮箱或密码不正确");else r.push('/admin')}return <form className="form" onSubmit={go}><input type="email" placeholder="管理员邮箱" value={e} onChange={x=>setE(x.target.value)} required/><input type="password" placeholder="密码" value={p} onChange={x=>setP(x.target.value)} required/><button className="gold">登录后台</button>{err&&<em>{err}</em>}</form>}